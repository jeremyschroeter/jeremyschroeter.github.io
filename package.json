{
  "name": "blog-static",
  "version": "1.0.0",
  "type": "module",
  "description": "Personal blog with Web Components",
  "main": "dist/blog-components.js",
  "scripts": {
    "build": "node scripts/generate-posts-json.js && rollup -c",
    "build:posts": "node scripts/generate-posts-json.js",
    "dev": "npm run build:posts && concurrently -k -n rollup,posts,serve -c blue,yellow,green \"rollup -c -w\" \"node scripts/watch-posts.js\" \"live-server --port=8000 --wait=200 --ignore=node_modules\"",
    "serve": "live-server --port=8000"
  },
  "author": "Jeremy Schroeter",
  "license": "MIT",
  "dependencies": {
    "katex": "^0.16.11"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.0",
    "@rollup/plugin-node-resolve": "^15.3.0",
    "bibtex-parse-js": "^0.0.24",
    "concurrently": "^9.2.3",
    "live-server": "^1.2.2",
    "prismjs": "^1.29.0",
    "rollup": "^4.28.0",
    "rollup-plugin-postcss": "^4.0.2"
  }
}
